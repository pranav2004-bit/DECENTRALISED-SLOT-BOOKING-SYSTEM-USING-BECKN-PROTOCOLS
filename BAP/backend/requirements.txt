Django==5.1.15
django-environ==0.11.2
psycopg2-binary==2.9.10
gunicorn==23.0.0
djangorestframework==3.15.2
django-redis==5.4.0
redis==5.2.1
requests==2.33.0
jsonschema==4.23.0
cryptography==48.0.1

# --- Test dependencies (per pyproject.toml addopts / TESTING.md — CI installs only this
# file, so these must live here, not in a separate requirements-dev.txt CI never reads) ---
pytest==9.0.3
pytest-django==4.9.0
pytest-cov==6.0.0
responses==0.25.3
