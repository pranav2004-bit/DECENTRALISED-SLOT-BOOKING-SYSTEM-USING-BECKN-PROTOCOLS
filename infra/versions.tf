terraform {
  required_version = ">= 1.7.0"

  # No provider block yet — see README.md for the activation trigger.
  # Once a cloud provider is chosen (Phase 3), add it here with a pinned version constraint.
}
